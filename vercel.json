{
  "buildCommand": "pnpm --filter bookkeeping-frontend build",
  "outputDirectory": "frontend/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
